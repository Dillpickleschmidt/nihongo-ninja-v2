"use server"

// import { createSupabaseAdmin } from "@/lib/supabase"

export async function createMember(data: { email: string; password: string }) {
  // const supabase = await createSupabaseAdmin()
  // create account
  // supabase.auth.admin.createUser({email: })
  // insert member info
  // intert into permissions
}
